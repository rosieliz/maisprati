/*
const ps = require("prompt-sync")();

const valor = Number(ps("Informe qual valor você quer multiplicar: "));

for (let i = 1; i <= 10; i++) {
    console.log(`${valor} × ${i} = ${valor*i}`)
}
*/

const arr = [
    [1, 2],
    ['a', 'b', 'c'],
    ["one", "two"]
];

console.table(arr);
