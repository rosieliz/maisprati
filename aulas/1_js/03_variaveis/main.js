console.log("Olá, mundo!");
console.log(2 + 5);

/* Este é um bloco
 * de comentários.
 * Abaixo estou declarando variáveis.
 */
var nome = "Liz";           // declaração de uma variável do tipo texto
let numero = 25;
let boolean = true;         // use quando o valor percisa mudar
const curso = "Full Stack"; // use quando o valor não muda

console.log(numero);
numero = 10;
console.log(numero);

console.log(curso);

if (true) {
    let mensagem = "ola";
    const curso2 = "jornalismo";
    consoe.log(mensagem);
    consoe.log(curso2);
}
